# 2026-07-07

## 更新概览

- React 模板：追平 pnpm、Vite、React Router DOM、Tailwind CSS、Radix UI、shadcn CLI、Vite DevTools、React lint 规则和开发辅助工具；锁文件按 pnpm 11.10.0 重新生成。
- Vue3 模板：追平 pnpm、Vite、TypeScript 6、Tailwind CSS、Lucide Vue、Oxc lint/format 工具链、Vite DevTools、Node 类型和 vue-tsc；为 TypeScript 6 的 `baseUrl` 弃用提示补充兼容配置。
- 本次验证：两个模板均删除旧 `pnpm-lock.yaml` 后使用 `pnpm@11.10.0 install` 重新安装；验证包含 peer 检查、lint、typecheck 和 build。

## React 模板 (vite_react_init)

### 依赖版本变更

| 依赖 | 旧版本 | 新版本 | 类型 | 影响说明 |
| --- | --- | --- | --- | --- |
| pnpm | 11.9.0 | 11.10.0 | toolchain | 包管理器基线追平，锁文件按 pnpm 11.10.0 重新生成。 |
| @tailwindcss/vite | 4.3.1 | 4.3.2 | patch | Tailwind Vite 插件补丁追平，现有 Vite 插件配置无需调整。 |
| autoprefixer | 10.5.0 | 10.5.2 | patch | CSS 后处理补丁追平，构建产物兼容性处理保持不变。 |
| immer | 11.1.8 | 11.1.11 | patch | 不可变数据工具补丁追平，模板状态写法无需调整。 |
| lucide-react | 1.22.0 | 1.23.0 | minor | 图标库追平，现有按需导入和 Tailwind 控制尺寸/颜色的写法保持兼容。 |
| radix-ui | 1.6.0 | 1.6.2 | patch | Radix primitives 补丁追平，shadcn/ui 基础组件源码无需迁移。 |
| react-router-dom | 7.18.0 | 7.18.1 | patch | React 路由补丁追平，路由守卫和懒加载页面注册方式不变。 |
| shadcn | 4.12.0 | 4.13.0 | minor | shadcn CLI 追平，组件生成目录和 `components.json` 约定不变。 |
| tailwindcss | 4.3.1 | 4.3.2 | patch | Tailwind CSS v4 补丁追平，现有主题变量和工具类写法保持兼容。 |
| @eslint-react/eslint-plugin | 5.10.0 | 5.11.2 | minor | React ESLint 规则追平，模板 lint 脚本继续使用当前 flat config。 |
| @vitejs/devtools | 0.3.3 | 0.3.4 | patch | Vite DevTools 补丁追平，仍通过环境变量按需启用。 |
| @vitejs/devtools-kit | 0.3.3 | 0.3.4 | patch | 随 DevTools 主包同步，保持开发调试工具链一致。 |
| @vitejs/devtools-self-inspect | 0.3.3 | 0.3.4 | patch | 随 DevTools 主包同步，现有配置无需改造。 |
| code-inspector-plugin | 1.6.2 | 1.6.5 | patch | 页面到源码定位工具补丁追平，调试快捷方式不变。 |
| react-grab | 0.1.47 | 0.1.48 | patch | React Grab 补丁追平，模板侧未调整使用方式。 |
| vite | 8.1.0 | 8.1.3 | patch | Vite 生产构建工具补丁追平，当前配置通过构建验证。 |

### 升级内容说明

- 框架与运行时：React / React DOM 保持 19.2.7；React Router DOM 从 7.18.0 追平到 7.18.1，当前路由守卫、404 页面和懒加载页面注册方式无需迁移。
- UI 与样式：Tailwind CSS 与 `@tailwindcss/vite` 追平到 4.3.2，Radix UI 和 shadcn CLI 同步更新；`src/shared/ui` 的组件源码和 Less/Tailwind 混用方式不变。
- 构建与工程化：Vite 追平到 8.1.3，Vite DevTools 体系追平到 0.3.4；`pnpm-lock.yaml` 重新生成，用于固化新的 Vite、Tailwind、Radix 和 DevTools 依赖图。
- 规范与开发辅助：@eslint-react 规则、code-inspector-plugin 和 react-grab 完成版本追平；未发现需要修改 ESLint 配置、Vite 配置或脚本入口的破坏性变更。
- 请求与工具库：Axios、ahooks、Zustand、React 和 TypeScript 版本保持不变；Immer、Autoprefixer 等 patch 升级属于补丁追平，模板业务代码无需调整。

### 模板影响

- README.md 同步 pnpm、Vite、React Router DOM、Tailwind CSS 和 lucide-react 基线；AGENTS.md 同步当前依赖基线。
- pnpm 安装自动更新 `pnpm-workspace.yaml` 的 `minimumReleaseAgeExclude`，新增 code-inspector 与 Radix 相关新版本白名单；这是供应链 release-age 策略的锁定结果。
- 本次未修改 React 模板源码、路由配置、状态管理、API 封装或 Vite 配置；未发现需要模板侧改造的破坏性变更。

## Vue3 模板 (vite_vue3_init)

### 依赖版本变更

| 依赖 | 旧版本 | 新版本 | 类型 | 影响说明 |
| --- | --- | --- | --- | --- |
| pnpm | 11.9.0 | 11.10.0 | toolchain | 包管理器基线追平，锁文件按 pnpm 11.10.0 重新生成。 |
| @lucide/vue | 1.22.0 | 1.23.0 | minor | Vue 图标库追平，现有按需导入方式保持兼容。 |
| @tailwindcss/vite | 4.3.1 | 4.3.2 | patch | Tailwind Vite 插件补丁追平，现有 Vite 配置无需调整。 |
| tailwindcss | 4.3.1 | 4.3.2 | patch | Tailwind CSS v4 补丁追平，主题变量和工具类写法保持兼容。 |
| @types/node | 26.0.1 | 26.1.0 | minor | Node 类型定义追平，脚本和 Vite 配置类型基线更新。 |
| @vitejs/devtools | 0.3.3 | 0.3.4 | patch | Vite DevTools 补丁追平，仍通过环境变量按需启用。 |
| @vitejs/devtools-kit | 0.3.3 | 0.3.4 | patch | 随 DevTools 主包同步，保持开发调试工具链一致。 |
| @vitejs/devtools-self-inspect | 0.3.3 | 0.3.4 | patch | 随 DevTools 主包同步，现有配置无需改造。 |
| autoprefixer | 10.5.0 | 10.5.2 | patch | CSS 后处理补丁追平，构建产物兼容性处理保持不变。 |
| code-inspector-plugin | 1.6.2 | 1.6.5 | patch | 页面到源码定位工具补丁追平，调试快捷方式不变。 |
| oxfmt | 0.56.0 | 0.58.0 | minor | Oxc formatter 追平，模板格式化命令保持不变。 |
| oxlint | 1.71.0 | 1.73.0 | minor | Oxc linter 追平，`--deny-warnings` 校验继续通过。 |
| typescript | 5.9.3 | 6.0.3 | major | TypeScript 升级到 6.0；模板保留现有 `baseUrl` 别名方案，并补充 `ignoreDeprecations: "6.0"` 通过类型检查。 |
| vite | 8.1.0 | 8.1.3 | patch | Vite 生产构建工具补丁追平，当前配置通过构建验证。 |
| vue-tsc | 3.3.5 | 3.3.6 | patch | Vue 类型检查工具补丁追平，TypeScript 6 下类型检查通过。 |

### 升级内容说明

- 框架与类型：Vue 保持 3.5.39，TypeScript 从 5.9.3 升级到 6.0.3；TypeScript 6 会提示 `baseUrl` 在未来版本弃用，模板已在 `tsconfig.json` 中加入 `ignoreDeprecations: "6.0"`，保持当前 `@/*` 路径别名行为不变。
- UI 与样式：Lucide Vue 追平到 1.23.0，Tailwind CSS 与 `@tailwindcss/vite` 追平到 4.3.2；shadcn-vue 组件源码目录和 reka-ui 使用方式保持不变。
- 构建与工程化：Vite 追平到 8.1.3，Vite DevTools 体系追平到 0.3.4；`pnpm-lock.yaml` 重新生成，用于固定新的 Vite、Tailwind、Oxc 和 DevTools 依赖图。
- Oxc 工具链：oxlint 从 1.71.0 追平到 1.73.0，oxfmt 从 0.56.0 追平到 0.58.0；`pnpm lint`、`pnpm format`、`pnpm format:check` 命令入口保持不变。
- 请求与状态：Axios、Pinia、Vue Router、VueUse 和 Vue DevTools 插件版本保持不变；本次未触及 HTTP 封装、Pinia 注册或路由聚合逻辑。

### 模板影响

- README.md 同步 pnpm、Vite 和 TypeScript 基线；AGENTS.md 同步当前依赖基线。
- `tsconfig.json` 新增 `ignoreDeprecations: "6.0"`，用于兼容 TypeScript 6 对 `baseUrl` 的弃用诊断；这是配置层兼容处理，不改变模板源码行为。
- pnpm 安装自动更新 `pnpm-workspace.yaml` 的 `minimumReleaseAgeExclude`，新增 code-inspector、oxfmt 和 oxlint 相关新版本白名单；这是供应链 release-age 策略的锁定结果。
- 本次未修改 Vue 模板业务源码、路由聚合、Pinia 注册、HTTP 封装或 Vite 配置；除 TypeScript 6 弃用提示兼容外，未发现需要模板侧改造的破坏性变更。

## 验证结果

- React 模板：
  - `npx pnpm@11.10.0 install`：通过；供应链策略检查通过，安装阶段有 npm registry 慢请求和 socket 重试提示。
  - `pnpm peers check --json`：通过，无 bad、missing、conflicts。
  - `npx pnpm@11.10.0 run lint`：通过，无 lint 错误。
  - `npx pnpm@11.10.0 run build`：通过，Vite 8.1.3 生产构建成功。
- Vue3 模板：
  - `npx pnpm@11.10.0 install`：通过；供应链策略检查通过，安装阶段有 npm registry 慢请求、socket 重试和 npm 临时包解包警告。
  - `pnpm peers check --json`：通过，无 bad、missing、conflicts。
  - `npx pnpm@11.10.0 lint`：通过，`oxlint --deny-warnings` 无提示。
  - `npx pnpm@11.10.0 typecheck`：首次因 TypeScript 6 的 `baseUrl` 弃用诊断失败；补充 `ignoreDeprecations: "6.0"` 后通过。
  - `npx pnpm@11.10.0 build`：通过，Vite 8.1.3 生产构建成功。

## 注意事项

- 本次升级以 2026-07-07 查询 npm registry 的最新稳定版本为准。
- 本机全局 `pnpm` 为 11.3.0，直接通过 `corepack pnpm run ...` 会触发 pnpm 版本检查失败；本次安装和验证均使用 `npx pnpm@11.10.0 ...` 执行，模板自身 `packageManager` 已更新为 pnpm 11.10.0。
- Vue 模板升级 TypeScript 6 后，`baseUrl` 会出现 TS5101 弃用提示；当前通过 `ignoreDeprecations: "6.0"` 保持既有路径别名方案，后续可评估改为不依赖 `baseUrl` 的 `paths` 配置。
- 安装和验证阶段仍可能看到 registry 慢请求、socket 重试、npm 临时包解包警告或第三方 deprecated subdependencies 提示；当前不影响安装、peer 检查、lint、typecheck 或生产构建。
