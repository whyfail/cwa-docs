# 2026-06-29

## 更新概览

- React 模板：追平 pnpm、Vite、Vite DevTools、旧浏览器兼容插件、React lint 规则、ESLint、Axios、Lucide React 和 shadcn CLI；同时移除上一轮为 Vite 8.0 peer 检查保守保留的 Axios / @eslint-react 固定策略。
- Vue3 模板：追平 pnpm、Vite、Vite DevTools、旧浏览器兼容插件、Vue DevTools、Vue、reka-ui、Lucide Vue 和 Node 类型；锁文件按新的 Vite 8.1 peer 范围重新解析。
- 本次验证：两个模板均删除旧 `pnpm-lock.yaml` 后重新执行 `pnpm install`；验证包含 `pnpm peers check`、lint/typecheck、build 和 cwa-docs 文档站构建。

## React 模板 (vite_react_init)

### 依赖版本变更

| 依赖 | 旧版本 | 新版本 | 类型 | 影响说明 |
| --- | --- | --- | --- | --- |
| pnpm | 11.8.0 | 11.9.0 | toolchain | 包管理器基线追平，锁文件按 pnpm 11.9.0 重新生成。 |
| axios | 1.17.0 | 1.18.1 | minor | HTTP 客户端追平，模板 `shared/api` 封装无需调整。 |
| lucide-react | 1.21.0 | 1.22.0 | minor | 图标库追平，现有按需导入和 Tailwind 样式写法保持兼容。 |
| shadcn | 4.11.0 | 4.12.0 | minor | shadcn CLI 追平，基础组件源码目录和生成约定不变。 |
| @antfu/eslint-config | 9.0.0 | 9.1.0 | minor | ESLint 预设追平，继续服务当前 flat config。 |
| @eslint-react/eslint-plugin | 5.8.19 | 5.10.0 | minor | React lint 规则追平，相关子包不再通过 pnpm overrides 固定在 5.8.19。 |
| @vitejs/devtools | 0.1.24 | 0.3.3 | minor | Vite DevTools 回到 Vite 8.1 要求的 `^0.3.0` peer 范围。 |
| @vitejs/devtools-kit | 0.1.24 | 0.3.3 | minor | 随 DevTools 主包同步，保持开发调试工具链一致。 |
| @vitejs/devtools-self-inspect | 0.1.24 | 0.3.3 | minor | 随 DevTools 主包同步，继续通过环境变量按需启用。 |
| @vitejs/plugin-legacy | 8.0.2 | 8.1.0 | minor | legacy 构建插件追平，旧浏览器兼容构建开关保持不变。 |
| eslint | 10.5.0 | 10.6.0 | minor | 代码规范工具链追平，模板 lint 脚本无需调整。 |
| vite | 8.0.16 | 8.1.0 | minor | 构建工具追平到 Vite 8.1，当前模板配置继续复用。 |

### 升级内容说明

- 框架与运行时：React / React DOM 保持 19.2.7，React Router DOM 保持 7.18.0；本次没有引入路由、状态或渲染入口迁移。
- 请求与工具库：Axios 追平到 1.18.1，并从上一轮的固定版本恢复为 `^1.18.1`；当前请求拦截、响应处理和错误封装无需改造。
- UI 与样式：Lucide React 和 shadcn CLI 完成 minor 追平，shadcn/ui 源码组件仍放在 `src/shared/ui`，Tailwind CSS v4 和 Less 配置不变。
- 构建与工程化：Vite 8.1.0 的 optional peer 已要求 `@vitejs/devtools@^0.3.0`，因此 DevTools 体系从 0.1.24 回到 0.3.3；`@vitejs/plugin-legacy`、ESLint、@antfu/eslint-config 和 @eslint-react 规则同步追平。
- 供应链策略：`pnpm-workspace.yaml` 更新 minimum release age 白名单，移除 Axios / @eslint-react 相关 overrides；`pnpm-lock.yaml` 重新生成以固化新的依赖图。

### 模板影响

- README.md 同步 pnpm、Vite、ESLint、Axios 和 Lucide React 版本基线；AGENTS.md 增加当前依赖基线说明。
- 本次未修改 React 模板业务源码、路由配置、状态管理、API 封装或 Vite 配置。
- 安装阶段 registry 多次出现 socket 重试和慢速下载提示，重试后安装成功；当前未发现需要模板侧改造的破坏性变更。

## Vue3 模板 (vite_vue3_init)

### 依赖版本变更

| 依赖 | 旧版本 | 新版本 | 类型 | 影响说明 |
| --- | --- | --- | --- | --- |
| pnpm | 11.8.0 | 11.9.0 | toolchain | 包管理器基线追平，锁文件按 pnpm 11.9.0 重新生成。 |
| @lucide/vue | 1.21.0 | 1.22.0 | minor | Vue 图标库追平，现有图标导入方式保持兼容。 |
| reka-ui | 2.10.0 | 2.10.1 | patch | shadcn-vue 底层 primitives 补丁追平，基础组件源码无需迁移。 |
| vue | 3.5.38 | 3.5.39 | patch | Vue 运行时补丁追平，模板入口、路由和 Pinia 注册无需调整。 |
| @types/node | 26.0.0 | 26.0.1 | patch | Node 类型定义补丁追平，工具脚本和 Vite 配置类型基线更新。 |
| @vitejs/devtools | 0.1.24 | 0.3.3 | minor | Vite DevTools 回到 Vite 8.1 要求的 `^0.3.0` peer 范围。 |
| @vitejs/devtools-kit | 0.1.24 | 0.3.3 | minor | 随 DevTools 主包同步，保持开发调试工具链一致。 |
| @vitejs/devtools-self-inspect | 0.1.24 | 0.3.3 | minor | 随 DevTools 主包同步，继续通过环境变量按需启用。 |
| @vitejs/plugin-legacy | 8.0.2 | 8.1.0 | minor | legacy 构建插件追平，旧浏览器兼容构建开关保持不变。 |
| vite | 8.0.16 | 8.1.0 | minor | 构建工具追平到 Vite 8.1，当前配置继续复用。 |
| vite-plugin-vue-devtools | 8.1.3 | 8.1.5 | patch | Vue DevTools 插件补丁追平，开发调试入口保持不变。 |

### 升级内容说明

- 框架与类型：Vue 从 3.5.38 补丁追平到 3.5.39，TypeScript 保持 5.9.3，`@types/node` 追平到 26.0.1；模板源码无需迁移。
- UI 与样式：reka-ui 2.10.1 和 @lucide/vue 1.22.0 继续支撑 shadcn-vue 基础组件和业务图标，Tailwind CSS v4 配置不变。
- 构建与工程化：Vite 8.1.0、@vitejs/plugin-legacy 8.1.0 和 Vite DevTools 0.3.3 同步追平；上次为 Vite 8.0 peer 清洁使用的 0.1.24 DevTools 版本不再适用。
- Vue DevTools：vite-plugin-vue-devtools 追平到 8.1.5，pnpm 安装时自动补充 `@vue/devtools-core`、`@vue/devtools-kit`、`@vue/devtools-shared` 8.1.5 到 minimum release age 白名单。
- 锁文件维护：`pnpm-lock.yaml` 删除后重新生成，用于固定 Vite 8.1、Vue 3.5.39 和 DevTools 体系的新解析结果。

### 模板影响

- README.md 补充 pnpm、Vite、Vue、Axios 和图标工具链基线；AGENTS.md 增加当前依赖基线说明。
- 本次未修改 Vue 模板业务源码、路由聚合、Pinia 注册、HTTP 封装或 Vite 配置。
- 首次安装因 npm registry 下载超时失败，原命令重试后成功；该问题属于网络下载超时，不是依赖冲突或模板配置错误。

## 验证结果

- React 模板：
  - `pnpm install`：通过；供应链策略检查通过，安装阶段有 registry socket 重试和慢速下载提示。
  - `pnpm peers check --json`：通过，无 bad、missing、conflicts。
  - `pnpm run lint`：通过，无 warning。
  - `pnpm run build`：通过，Vite 8.1.0 生产构建成功。
- Vue3 模板：
  - `pnpm install`：首次因 registry 下载超时失败，重试通过；供应链策略检查通过。
  - `pnpm peers check --json`：通过，无 bad、missing、conflicts。
  - `pnpm lint`：通过，`oxlint --deny-warnings` 无提示。
  - `pnpm typecheck`：通过，`vue-tsc --noEmit -p tsconfig.json` 无类型错误。
  - `pnpm build`：通过，Vite 8.1.0 生产构建成功。
- cwa-docs：
  - `pnpm build`：通过，VitePress 站点构建成功；安装前供应链策略检查通过，仅出现一次 npm registry 慢请求提示。

## 注意事项

- 本次升级以 2026-06-29 查询 npm registry 的最新稳定版本为准。
- Vite 8.1.0 的 optional peer 已转向 `@vitejs/devtools@^0.3.0`，两个模板均同步使用 DevTools 0.3.3。
- React 模板移除了 Axios / @eslint-react 的 pnpm overrides，后续如 minimum release age 策略再次拦截新包，需要优先以供应链策略输出为准。
- 安装阶段仍可能看到第三方 deprecated subdependencies 提示，来源于上游间接依赖；当前不影响安装、peer 检查、lint、typecheck 或生产构建。
